#include <iostream>
#include "math_utils.h"

int main(){
    int x = 2, y = 5;

    std::cout << "soma: " << soma(x,y) << "\n";
    std::cout << "multiplicação: " << mult(x,y) << "\n";

    return 0;
}